<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.gabriel.planetsinfo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_planets"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_planets_foreground"
        android:supportsRtl="true"
        android:theme="@style/Theme.PlanetsInfo">

        <activity android:name=".activity.MainActivity" android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".activity.DetailActivity"
            android:parentActivityName=".activity.MainActivity"/>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>